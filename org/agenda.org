#+TITLE: Agenda - Artificial Intelligence
#+AUTHOR: Marcus Birkenkrahe
#+SUBTITLE: COSC 3430 (AI)Fall 2026 | Augustana U.
#+STARTUP: overview hideblocks indent entitiespretty
#+OPTIONS: toc:nil num:nil ^:nil
[[../img/breughel_icarus.jpg]]
Image: Landscape with the Fall of Icarus by Pieter Bruegel the Elder
(1560)

* DONE Preamble: The Fall of Icarus

- What's this painting about?

- What has this to do with AI?

* DONE W1S1: Overview of the course

|   | Topic                                                        |
|---+--------------------------------------------------------------|
| x | Welcome & objectives                                         |
| x | Get to know your teacher & share why you're here             |
| x | Understand what this course is about                         |
|   | Find out where to find stuff: Canvas, GitHub, textbooks etc. |
|   | Whom to ask, formalities, and grading                        |
|   | The project: Scrum sprints, tracks, and topic ideas          |
|   | Weekly rhythm (in-class, exams, at-home) & setup             |
|   | Assignments & what's next                                    |
|---+--------------------------------------------------------------|

File: See =org/1_overview.org= in GitHub

* DONE W1S2: Finish overview/projects

- Short check-in: What (if anything) do you remember from last time?

- I sorted the Google Space problem out (with the help of IT): You
  should have received an invitation from me.

- I added the link to the class photo to Canvas in the Google Space:
  did I get it right? ([[https://docs.google.com/presentation/d/1BU6DaMT_l4J-lS6OvxLjBkuAryi1qO1cXFhCPdvC8Wg/edit?usp=sharing][Link]])

- If there's time, we start with an excursion into Colab + Python.

|   | Topic                                               |
|---+-----------------------------------------------------|
| x | Use of generative AI                                |
| x | Whom to ask, formalities, and grading               |
| x | The project: Scrum sprints, tracks, and topic ideas |
| x | Weekly rhythm (in-class, exams, at-home) & setup    |
| x | Assignments & what's next                           |
|---+-----------------------------------------------------|

* DONE W2S1: Colab + Python 1: Jupyter notebooks, Python packages

** Housekeeping

- I sent you a long (weekly) email (twice, by mistake). Did you get
  it/read it? You may have to change your Canvas settings: Account >
  Notifications > Announcements > Notify immediately.

- This week's programming assignment follows the Colab + Python
  code-along lecture - due Friday September 4 (11:59 pm).

- Since we won't finish this project until Wednesday, you should
  already start with the assignment (in Colab) and get as far as we
  did in class.

** Colab [Google Colaboratory] + Python [python3]

- *Why Colab?* JupyterLite is good for most purposes (unless you need
  free GPU computing time); it does not have Gemini (generative AI),
  and it does not save files automatically in your GDrive.

- Let's make a simple plot with Python in Colab and learn a few
  things about Python along the way.

  1) Import ~pandas~ library for data frame manipulation.
  2) Download the ~Iris~ dataset from Kaggle.
  3) Load dataset into a ~pandas~ ~DataFrame~.
  4) Include exception handling with ~try~ ~except~.
  5) Explore the dataset
  6) Create a simple plot
  7) Summarize and check your understanding

- Along the way, you're also going to pick up:
  1) Create text cells with markdown language.
  2) Create and run code cells.
  3) Use the terminal (aka shell).
  4) Understand Jupyter controls and settings.
  5) Use Gemini AI in Colab.

- Open  https://colab.research.google.com

- Code along with me!

- Upload your final Colab .ipynb file no later than Monday Sept 7.

* DONE W2S2: Colab + Python 2: get and explore a dataset

** Review

The review usually is for you to check in with yourself if you know
the answer. Sometimes it'll come in the form of a surprise
quiz. Sometimes it give additional details last forgotten.

1) Where is the Colab file that you worked on last time?
   #+begin_quote
   A copy of the ~.ipynb~ notebook is stored on your personal GDrive, in
   a folder called ~Colab notebooks~.
   #+end_quote

2) Where does the name "Jupyter notebook" come from?
   #+begin_quote
   Jupyter notebooks offer three runtime types (language kernels): for
   Julia, Python, and R. You can change this in the ~Runtime~ menu. When
   you run another language, the chosen kernel serves as a wrapper to
   create files in that language (with the ~%%writefile~ command).
   #+end_quote

3) What is "Markdown"?
   #+begin_quote
   It is a lightweight *markup* language when editing with data + layout
   separated (as opposed to WYSIWIG = What You See Is What You Get).

   You use plain, readable characters instead of a visual editor or raw
   HTML (= HyperText Markup Language).

   For example: =##= generates a level-2 headline, =**= tags bold face,
   and =[text](https://...)= creates an inline link for =text=.
   #+end_quote

4) What's a ~DataFrame~?
   #+begin_quote
   A ~DataFrame~ is a two-dimensional table of data with labeled rows
   and columns. Each column can hold a different data type, every
   value within a column shares the same data type. Rows and columns
   have labels (row-index and column-name). It supports operations
   like: filtering rows, sorting, grouping, aggregating, joining,
   handling missing values, and reshaping the data. In Python, it is
   implemented in the ~pandas~ 
   #+end_quote
   Example:
   #+begin_src python :results output :exports both :session *Python* :python python3 
     import pandas as pd  # import pandas library

     df = pd.DataFrame({  # define DataFrame
         "name": ["Alice", "Bob", "Carol"],   # define a `name` column
         "age": [30, 25, 35],                 # define a `age` column
         "city": ["NYC", "LA", "Chicago"]     # define a `city` column
         })

     print(df[df["age"] > 27]) # filter rows where `age` is greater than 27
   #+end_src

5) What's a *list comprehension* in Python?
   #+begin_quote
   A ~list~ is a fundamental data structure in Python. It is actually
   not implemented like a list in C++ or Java but as a dynamic array,
   closer to the ~std::vector~ class in C++ (contiguous memory storage).

   A *list comprehension* is a compact way to build a list by *looping*
   over an *iterable* and optionally filtering/transforming its items,
   all in a single line instead of writing a ~for~ loop with ~.append~.
   #+end_quote
   Example:
   #+begin_src python :results output :exports both :session *Python* :python python3 
     # squares from 0 to 9 as list comprehension
     squares = [x**2 for x in range(10)]
     print(squares)

     # squares from 0 to 9 with a for loop:
     squares2 = []
     for x in range(10):
         squares2.append(x**2)

     print(squares2)
   #+end_src

   #+RESULTS:
   : [0, 1, 4, 9, 16, 25, 36, 49, 64, 81]
   : [0, 1, 4, 9, 16, 25, 36, 49, 64, 81]
   
** Colab + Python part 2

- We continue with our [p]review of Colab notebooks and Python.

- Open the notebook you were working on last.

- Alternatively, open my notebook and make a copy to edit it:
  https://tinyurl.com/colab-demo-ipynb

- You will have to upload your notebook at the end of this session.

* DONE W2S3: Colab + Python 3: Explore and visualize a DataFrame
#+attr_html: :width 400px :float nil:
#+caption: Explainer infographics generated by NotebookLM
[[../img/Data_Science_Programming_Workflow_Guide.png]]

Image: NotebookLM infographic for Colab + Python (GitHub)

** Bonus content (Google space)
Highlighting some bonus content for this class ([[https://chat.google.com/room/AAQAkkjACa4?cls=7][from the chat]]):
1. Tutorial to ~pandas~ library (~DataFrame~ / table manipulation)
2. Installing Python's package manager ~pip~ on a Mac
3. Mathematics in the Age of AI by Terence Tao (2026)
4. The original ELIZA chatbot paper by Weizenbaum (1966)
5. Using AI agent harnesses in bioinformatics research (seminar)
6. Opinion piece: Losing the craft of coding because of AI
7. Paper by Vaswani et al (2017) that started generative AI
8. YouTube Playlist with PyTorch in 100 seconds and more.

** Review

1) What are the three levels ("stateful layers") we're operating on
   when using Jupyter?
   #+begin_quote
   1. The notebook (.ipynb, Jupyter server)
   2. Python session (kernel, .py)
   3. OS environment (shell, environment)
   #+end_quote
2) What's a Python ~list~?
   #+begin_quote
   A Python list is a built-in, mutable, ordered sequence types that
   stores an indexed collection of references to objects.

   It's implemented as a dynamic array (contiguous block of pointers
   to objects in memory). Index access performs O(1) (a[i] is just
   base_address + i * pointer_size, while linked list performs O(n) -
   must walk from the head, node by node.
   #+end_quote
3) How can you extract the =age= column from a dataframe =df=?
   #+begin_src python :results output :exports both :session *Python* 
     import pandas as pd

     df = pd.DataFrame({
         "age" : [30,25,27]
         })
     print(df)
     print(df["age"]) # the footer is the metadata of the Series
     print(df["age"].to_string(index=False))     
   #+end_src

   #+RESULTS:
   #+begin_example
      age
   0   30
   1   25
   2   27
   0    30
   1    25
   2    27
   Name: age, dtype: int64
   30
   25
   27
   #+end_example

4) How do I filter only the fields with age smaller or equal to 27?
   #+begin_src python :results output :exports both :session *Python* :python python3 
     print(df["age"] <= 27)  # boolean flag vector
     print(df[df["age"] <= 27])  # boolean flag vector     
   #+end_src

   #+RESULTS:
   : 0    False
   : 1     True
   : 2     True
   : Name: age, dtype: bool
   :    age
   : 1   25
   : 2   27
5) In Python, division by zero generates a ~ZeroDivisionError~
   exception. How can you guard against that?
   #+begin_src bash :results output :exports both
     python3 -c "1/0" 2>&1  # bash shell command
   #+end_src

   #+RESULTS:
   : Traceback (most recent call last):
   :   File "<string>", line 1, in <module>
   :     1/0
   :     ~^~
   : ZeroDivisionError: division by zero

   #+begin_src python :results output :exports both :session *Python* :python python3
     x = 0
     try:                     
         print(1/x)
     except ZeroDivisionError as e:  # e is an alias 
         print(f"{e}")
   #+end_src

   #+RESULTS:
   : division by zero

** Colab + Python part 3

- We continue with our [p]review of Colab notebooks and Python.

- Open the notebook you were working on last.

- Alternatively, open my notebook and make a copy to edit it:
  https://tinyurl.com/colab-demo-ipynb

- *Upload your updated notebook to Canvas at the end of the session.*

** Take away

** Long weekend

- Look at chapter 1 of the textbook by Géron.
- Look at "Understanding Machine Learning" in DataCamp.
- Look at the AI explainer video (linked in Canvas).
- Think about a project you might like to do.
- Assignment: Complete the Palmer Penguins assignment.
  
* DONE W3S1: Machine Learning Fundamentals I (Overview)

** Housekeeping
#+attr_html: :width 400px :float nil:
[[../img/linux.png]]

- Code-along: everybody submitted, most succeeded. Make sure your code
  is actually working before you submit. I check every submission.

  | Bucket                     | Students |
  |----------------------------+----------|
  | Did not submit             | none     |
  | Grouped-plot bug (step 12) | ...      |
  | Minor issues               | ...      |
  | Clean                      | ...      |

  10 of 10 registered students submitted. All were present on
  2026-09-04.

- You need to submit correct code-along files so that I can value your
  participation, and as a record of your own progress in the class.
  
- You should try Linux, especially if you're Windozed: see my new
  [[https://claude.ai/code/artifact/a4345a76-77d9-46d1-acf8-f01d12b6acdf][tutorial for absolute beginners]].

** Project ideas?

- Anything?
  
- Will open the Sprint 1 assignment ("Project proposal") this week

- Deadline: Friday, 9/25

** Machine Learning Fundamentals:

Topics:
- [X] Why ML?
- [X] What is it?
- [ ] Simple example: spam filter
- [ ] Types of ML
- [ ] Simple example: diagnose illness (home assignment)
- [ ] ML workflow
- [ ] Simple example: linear regression (practice assignment)

* DONE W3S2: Machine Learning Fundamentals II (Types of ML)

Topics:
- [X] Why ML?
- [X] What is it?
- [X] Simple example: spam filter
- [X] Types of ML
- [X] Simple example: diagnose illness (home assignment)
- [ ] ML workflow
- [ ] Simple example: linear regression (practice assignment)

** Bonus content (Google Space)

- Tutorial introduction to ~pandas~ data structures
- Free online book Statistics and Machine Learning in Python (2025)
- Opinion: a physicist (string theory) on being successful with AI
- Video overview and infographic on Colab & Python
- Article on the math necessary for machine learning
- Tutorial on getting started with Linux on Windows (WSL)
- Getting ideas for a project (job interview scenario)
- Arthur L. Samuels article on an ML model playing checkers (1959)
- Project ideas (especially for finance but the method transfers)
- Posted the first sprint assignment (project proposal)

* DONE W4S1: Machine Learning Fundamentals III (Decision trees)

- [X] Why ML?
- [X] What is it?
- [X] Simple example: spam filter
- [X] Types of ML
- [X] Simple example: diagnose illness (home assignment)
- [X] ML workflow
- [ ] Simple example: linear regression (practice assignment)

* DONE W4S2: Simple linear regression example

- [X] Review: ML Workflow
- [X] Fitting a linear model to data
- [X] Making predictions
- [X] Visualizing the model
- [X] Practice fitting a linear model to data (with Gemini)

** Review: ML workflow

1. Remember any of the steps of the ML workflow - or all of them?
   #+begin_quote
   1) Define the problem
   2) Collect data
   3) Prepare data
   4) Choose a model
   5) Train the model (includes splitting train/test data),
   6) Evaluate the model
   7) Use the model for prediction (production).
   #+end_quote

2. The workflow is described as *iterative*. Give a concrete example
   of when you'd have to go back to an earlier step.
   #+begin_quote
   Any example where evaluation reveals a problem: e.g. the model
   underfits (step 6), so you go back to step 4 and choose a more
   complex model, or back to step 3 to engineer better features; or
   the model overfits, so you go back to step 2 to collect more data,
   or step 5 to constrain training (e.g. a shallower decision tree).
   #+end_quote

3. What's the difference between *overfitting* and *underfitting*, and
   at which workflow step would you actually catch either one
   happening?
   #+begin_quote
   Overfitting: the model learns noise instead of the underlying
   pattern -- it does great on training data but poorly on new, unseen
   data.

   Underfitting: the model is too simple to capture the real structure
   -- it does poorly even on the training data.

   Both are caught at step 6, Evaluate the model, specifically by
   checking performance on the held-out *test* data rather than the
   training data: a perfect training-set score doesn't rule out either
   problem (it can't reveal overfitting at all, since that's exactly
   the data the model memorized).
   #+end_quote

4. In the diagnosing-illness exercise, the patient data and rules were
   already given to you. Which workflow step did that exercise actually
   make you practice, and which steps did it skip entirely?
   #+begin_quote
   It practiced steps 4-7: choosing a model (a decision tree),
   training it (building the tree from the patient table),
   evaluating/using it (testing on Patients 11-13, then 14-17). Steps
   1-3 -- define the problem, collect data, prepare data -- were
   skipped, since the problem was already framed and the patient table
   was handed to you ready to use.
   #+end_quote


* W4S3: Instance-based machine learning (cosine similarity)

Four weeks in, here's the tally:

|-----------------------------------+-----|
| Class sessions so far             |  10 |
| Pages of material covered         | ~41 |
| Lines of Python code you've seen  | 415 |
| Lines of it you ran live in class | 345 |
| Class attendance index            | 93% |
|-----------------------------------+-----|

- [X] Review: Linear regression simple example
- [ ] Projects update (deadline Sept 25)
- [ ] Cosine similarity measure (demo/code along)
- [ ] Summary: ML Fundamentals

** How to read, how to study, how to learn programming

I'm going to make a video on this, I think, because it's important:

1) You don't learn by *grazing* (like a cow eat grass)

2) You don't learn by *gorging* (like a shark)

3) You don't learn by *gallivanting* (like a tourist)

4) You only learn by *grappling* with the material (like a wrestler)

5) How does one immerse oneself deeply (in CS)?
   #+begin_quote
   - Read the same passage (or code) again and again until it makes
     sense to you (or until you understand why it does not make sense)
   - Practice in small daily doses rather than cramming once a week --
   - Write code every day, even for ten minutes, the way you'd practice an instrument.
   - Struggle with a problem for a while before looking  up (or asking AI for) the answer: the friction is where the   learning happens, not the solution itself.
   - Produce, don't just consume -- reading someone else's code or
     watching a video feels like progress but doesn't build the same
     muscle as typing it yourself, breaking it, and fixing it.
   - Explain what you wrote to someone else, or out loud to yourself,
     or to the AI: if you can't explain it, you haven't actually
     learned it yet, no matter how cleanly it compiles.
   #+end_quote


** Review: Simple linear regression example

1. Before fitting the model, we reshaped the =hours= array from shape
   =(5,)= to =(5, 1)= with =np.reshape(-1, 1)=. Why is this necessary?
   #+begin_quote
   scikit-learn's ~LinearRegression~ (and most ML models) expect the
   input feature matrix =X= in 2D form: rows are samples, columns are
   features. A plain 1D array of 5 values doesn't specify whether
   that's 5 samples of 1 feature or 1 sample of 5 features, so
   ~reshape(-1, 1)~ makes it explicit: 5 rows, 1 column.

   Why these two arguments? The second argument, =1=, fixes the number
   of columns to 1 (one feature). The first argument, =-1=, tells
   NumPy "figure out this dimension yourself" from the array's total
   size and the other dimension(s) given -- here, 5 elements / 1
   column = 5 rows. Using =-1= means you don't have to hardcode the
   sample count, which matters once the dataset's size can change.
   #+end_quote

2. Once we have a ~LinearRegression~ instance, how do we actually train
   it on the hours/score data, and how do we read the two learned
   parameters back out afterward?
   #+begin_quote
   We call =model.fit(hours, score)= -- =hours= is =X= (input), =score= is =y=
   (output). After fitting, the slope is =model.coef_[0]= (here 7.5) and
   the intercept is =model.intercept_= (here 41.5). Together they define
   =prediction = intercept + slope * x=
   #+end_quote
   
3. What exactly does =.fit()= do -- what quantity is it minimizing?
   #+begin_quote
   It solves an optimization problem: it finds $\beta_0$ and $\beta_1$ that
   minimize the sum of squared residuals (the least-squares error)
   between the actual =y= values and the model's predictions:
   #+end_quote
   #+attr_html: :width 250px :float nil: 
   [[../img/residual.png]]

4. The dataset we used had 5 data points and only 2 unknown
   parameters. What does this mean mathematically and practically?
   #+begin_quote
   Each data point is a constraint (an equation the line should
   satisfy), so we have 5 equations but only 2 unknowns ($\beta_0$, $\beta_1$)
   -- more constraints than free parameters. This is called an
   *overdetermined system*: in general no line can satisfy all 5
   exactly.

   Why not? A line has only 2 degrees of freedom -- fixing $\beta_0$ and
   $\beta_1$ from any 2 of the points already determines the line
   completely, leaving no freedom to also pass through the
   other 3. Unless all 5 points happen to be exactly collinear (which
   real, noisy data almost never is), the remaining points will fall
   off that line. Practically, that's fine -- we don't look for an
   exact fit, we pick the line that minimizes the total squared error
   across all points (least squares), accepting a small residual at
   each one.
   #+end_quote
   #+attr_html: :width 150px :float nil:
   [[../img/overdetermined.png]]
   
5. How do we use the trained model to predict a score for someone who
   studies 6 hours? Why can't we just pass in a plain number like =6=?
   #+begin_quote
   We call =model.predict([[6]])= -- not =model.predict(6)= or
   =model.predict([6])=. ~predict~ expects input in the same shape as the
   training data: a 2D array of shape =(n_samples, n_features)=.  =[[6]]=
   is one sample with one feature, shape =(1, 1)=; a plain =6= or =[6]=
   would not match, which scikit-learn rejects. The result is also
   always a sequence, even for one input: =array([86.5])=, i.e. a
   predicted score of 86.5. You can check with ~np.shape()~.
   #+end_quote


** Projects: Don't forget the deadline for the proposal (Sept 25)

- Any ideas so far?

  + Research reinforcement learning
  + Route optimization with dynamic constraints
  + Image classification (bird species)


** Cosine similarity (demo and assignment)

- You're going to work on instance-based ML in your assignment

- The assignment has 4D, but we demonstrate the concept in 2D

- Open in Colab to code along: [[https://tinyurl.com/cosine-similarity][tinyurl.com/cosine-similarity]]

- For interactive demo, check out [[https://birkenkrahe.github.io/ml_demos][birkenkrahe.github.io/ml_demos]]

** Summary: Week 4 -- Machine Learning Fundamentals

- *Decision trees*: rules built from labeled examples (illness
  diagnosis); a single tree depends on whatever data/features you
  happened to use, so a *random forest* combines many trees for a
  sturdier answer.

- *The ML workflow*: define the problem, collect data, prepare data,
  choose a model, train, evaluate, predict -- and it's *iterative*: a
  bad evaluation sends you back to an earlier step.

- *Overfitting vs. underfitting*: an overfit model chases noise in the
  training data and fails on new data; an underfit model is too simple
  to capture the pattern at all -- both are caught at the evaluation
  step.

- *Linear regression*: fitting a straight line by minimizing the sum
  of squared residuals (least squares). With more data points than
  parameters, the system is generally *overdetermined* -- no line hits
  every point exactly, so we accept the best overall fit instead.

- *Cosine similarity*: comparing two feature vectors by the *angle*
  between them rather than their size -- the idea behind the
  *k-Nearest-Neighbor* method we start next.

Across all of it: a model is a function learned from data that makes
predictions on new data -- trees, lines, and angles this week are just
different ways of building that function.


** NEXT k-Nearest-Neighbor (kNN) machine learning 

